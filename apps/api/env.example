# Copy this file to `apps/api/.env` (it is gitignored) and fill in the paths.

# Existing snapshot DB (read-only)
SOURCE_DB_PATH="C:\\Users\\ThomasYtterstrom\\AppData\\Local\\HusqvarnaServiceHubData\\data.db"

# App-owned sidecar DB (read-write)
METADATA_DB_PATH="./data/product-analyzer-metadata.db"

# Optional
PORT=5174
